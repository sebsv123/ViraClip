# Rust Agent

Optional high-performance sidecar service used by ViraClip for specialized task execution.

It is wired in `docker-compose.yml` as the `rust-agent` service and can be configured with `RUST_AGENT_URL`.
